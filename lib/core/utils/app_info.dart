/// Identité de l'app — nom encore provisoire, à changer ici uniquement.
/// (Le nom sous l'icône est configuré à part : `android:label` dans
/// AndroidManifest.xml et `CFBundleDisplayName` dans ios/Runner/Info.plist.)
class AppInfo {
  AppInfo._();

  static const String name = 'Ama';
  static const String tagline = "Plantes médicinales d'Afrique";

  static const String medicalDisclaimer =
      'Les conseils contenus dans cette application ne représentent aucunement '
      'une consultation médicale et ne peuvent pas se substituer à elle.';
}
